services:
  web:
    image: sefboonen/carevoice-web:latest
    ports:
      - "3000:3000"
    environment:
      - WHISPER_SERVER_URL=ws://your-whisper-server.com:9000  # Production Whisper server
    restart: unless-stopped
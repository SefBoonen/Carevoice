services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: dev
    ports:
      - "3000:3000"
    environment:
      - WHISPER_SERVER_URL=ws://77.104.167.148:40353
      - LLM_SERVER=http://77.104.167.148:40465/v1/
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend